server:
  port: 12000

spring:
  mvc:
    view:
      suffix: .html
  thymeleaf:
    prefix: classpath:/templates
  servlet:
    multiPart:
      max-file-size: 10MB
  datasource:
    url: jdbc:mariadb://35.216.1.250:3306/messenger?characterEncoding=UTF-8&serverTimezone=UTC
    driver-class-name: org.mariadb.jdbc.Driver
    username: teamride
    password: 1234
   
#mybatis:
#  mapper-locations:
#  - classpath:mappers/**/*.xml
#  configuration:
#    map-underscore-to-camel-case: true

logging:
  logback:
    rollingpolicy:
      max-file-size: 100MB
      max-history: 7
  level:
    root: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} [%4L] - %m%n"
    file: "[%d{HH:mm:ss.SSS}][%-5level][%t][%logger.%method:line%line] - %msg%n"
  file:
    name: "${messenger.home:/tmp}/logs/messenger-server.log"